---
phase: 03-game-detection
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src-tauri/Cargo.toml, src-tauri/src/commands/games.rs, src-tauri/src/lib.rs, package.json, src/types/ipc.ts, src/lib/tauri.ts, src/lib/dialog.ts, src/lib/games.ts, src/stores/games.ts, src/i18n/messages.ts, src/components/games/GameSourceBadge.tsx, src/components/games/GameLibraryTable.tsx, src/components/games/ManualGameRegistrationForm.tsx, src/pages/LibraryPage.tsx, src/pages/GamesPage.tsx, src/index.css, src/pages/LibraryPage.test.tsx, src/components/games/GameLibraryTable.test.tsx, src/components/games/ManualGameRegistrationForm.test.tsx]
autonomous: true
requirements: [GAME-01, GAME-02, GAME-04, GAME-05, GAME-06]

must_haves:
  truths:
    - "The app persists a reconciled game library in SQLite, combining Steam detections with manual registrations while promoting duplicates to Steam origin"
    - "LibraryPage becomes a compact, management-first list with visible Steam/manual badges, refresh action, add-game flow, and manual-only remove affordances"
    - "Manual registration uses a native executable picker plus a confirmation form before saving"
    - "All new Phase 3 library-management copy is sourced from the i18n catalogs in English and pt-BR"
  artifacts:
    - path: "src-tauri/src/commands/games.rs"
      provides: "SQLite-backed manual registration, duplicate merge logic, and manual removal rules"
      min_lines: 260
      contains: "register_manual_game"
    - path: "src/stores/games.ts"
      provides: "Library load state and frontend actions for refresh, add, and remove"
      contains: "refreshLibrary"
    - path: "src/pages/LibraryPage.tsx"
      provides: "Compact management-first game-library UI"
      min_lines: 120
      contains: "GameLibraryTable"
    - path: "src/components/games/ManualGameRegistrationForm.tsx"
      provides: "Confirmation form for manual game registration"
      contains: "displayName"
    - path: "src/i18n/messages.ts"
      provides: "Bilingual copy for source badges, table labels, empty states, and manual registration controls"
      contains: "Refresh library"
  key_links:
    - from: "src/lib/dialog.ts"
      to: "src/pages/LibraryPage.tsx"
      via: "native executable selection before opening the confirmation form"
      pattern: "pickGameExecutable"
    - from: "src/stores/games.ts"
      to: "src/lib/tauri.ts"
      via: "refresh/add/remove wrappers"
      pattern: "refreshGameLibrary"
    - from: "src/pages/LibraryPage.tsx"
      to: "src/components/games/GameLibraryTable.tsx"
      via: "management list composition"
      pattern: "GameLibraryTable"
    - from: "src/components/games/GameLibraryTable.tsx"
      to: "src/components/games/GameSourceBadge.tsx"
      via: "visual source distinction"
      pattern: "GameSourceBadge"
---

<objective>
Build the persisted library-management layer and compact frontend for Phase 3: manual executable registration, SQLite-backed merge rules, source badges, refresh/remove actions, and focused frontend tests.

Purpose: Turn backend Steam detections into the user-facing library management flow required by the roadmap while keeping richer metadata UI deferred to Phase 4.
Output: SQLite-backed library commands, native add-game flow, compact management list in `LibraryPage`, and frontend tests for source badges plus manual-only removal.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-detection/03-CONTEXT.md
@.planning/phases/03-game-detection/03-RESEARCH.md
@.planning/phases/03-game-detection/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add native executable picking and SQLite-backed manual game commands</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/commands/games.rs, src-tauri/src/lib.rs, package.json, src/types/ipc.ts, src/lib/tauri.ts</files>
  <read_first>src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/src/commands/games.rs, package.json, src/types/ipc.ts, src/lib/tauri.ts, .planning/phases/03-game-detection/03-CONTEXT.md, .planning/phases/03-game-detection/03-RESEARCH.md</read_first>
  <action>
Extend `src-tauri/Cargo.toml` with these exact persistence and dialog dependencies if missing:

```toml
rusqlite = { version = "0.39.0", features = ["bundled"] }
tauri-plugin-dialog = "2.6.0"
```

Extend `package.json` dependencies with:

```json
"@tauri-apps/plugin-dialog": "^2.0.0"
```

Update `src-tauri/src/lib.rs` so the builder initializes the dialog plugin exactly as:

```rust
.plugin(tauri_plugin_dialog::init())
```

Extend `src/types/ipc.ts` with:
- `ManualGameRegistrationInput` fields `displayName` and `executablePath`
- `GameLibraryEntry` field `removable: boolean`

Extend `src/lib/tauri.ts` with these exact wrappers:

```ts
export async function registerManualGame(
  input: ManualGameRegistrationInput,
): Promise<GameLibraryEntry> {
  return tauriInvoke<GameLibraryEntry>('register_manual_game', { input })
}

export async function removeManualGame(gameId: string): Promise<void> {
  return tauriInvoke<void>('remove_manual_game', { gameId })
}
```

Update `src-tauri/src/commands/games.rs` so it implements these exact commands:

```rust
#[tauri::command]
pub async fn register_manual_game(
    app: tauri::AppHandle,
    input: ManualGameRegistrationInput,
) -> Result<GameLibraryEntry, AppError>

#[tauri::command]
pub async fn remove_manual_game(
    app: tauri::AppHandle,
    game_id: String,
) -> Result<(), AppError>
```

Persist records in SQLite under the app data directory using a single table named `game_library` with these exact columns:
- `id TEXT PRIMARY KEY`
- `display_name TEXT NOT NULL`
- `executable_path TEXT NOT NULL UNIQUE`
- `install_dir TEXT NOT NULL`
- `source TEXT NOT NULL`
- `steam_app_id INTEGER`
- `removable INTEGER NOT NULL`
- `last_seen_at TEXT`

Update `refresh_game_library(app)` to reconcile the SQLite rows with the latest Steam scan:
- Steam matches on normalized executable path or normalized install root promote existing manual rows to `source = 'steam'`
- promoted or Steam-origin rows must set `removable = false`
- rows that remain purely manual keep `removable = true`
- Steam rows not seen in the latest refresh are removed from the returned/persisted detected library
  </action>
  <verify>Check that dialog/plugin dependencies exist, lib.rs initializes tauri_plugin_dialog, tauri.ts exports registerManualGame/removeManualGame, and games.rs defines SQLite-backed register/remove commands</verify>
  <acceptance_criteria>
    - src-tauri/Cargo.toml contains 'rusqlite = { version = "0.39.0", features = ["bundled"] }'
    - src-tauri/Cargo.toml contains 'tauri-plugin-dialog = "2.6.0"'
    - package.json contains '"@tauri-apps/plugin-dialog"'
    - src-tauri/src/lib.rs contains 'tauri_plugin_dialog::init'
    - src/lib/tauri.ts contains 'registerManualGame'
    - src/lib/tauri.ts contains 'removeManualGame'
    - src-tauri/src/commands/games.rs contains 'register_manual_game'
    - src-tauri/src/commands/games.rs contains 'remove_manual_game'
    - src-tauri/src/commands/games.rs contains 'game_library'
    - src/types/ipc.ts contains 'removable: boolean'
  </acceptance_criteria>
  <done>Native file selection and SQLite-backed manual game management commands are available</done>
</task>

<task type="auto">
  <name>Task 2: Build the frontend store, compact management components, and bilingual library copy</name>
  <files>src/lib/dialog.ts, src/lib/games.ts, src/stores/games.ts, src/i18n/messages.ts, src/components/games/GameSourceBadge.tsx, src/components/games/GameLibraryTable.tsx, src/components/games/ManualGameRegistrationForm.tsx, src/index.css</files>
  <read_first>src/i18n/messages.ts, src/index.css, src/lib/tauri.ts, src/types/ipc.ts, src/stores/hardware.ts, .planning/phases/03-game-detection/03-CONTEXT.md</read_first>
  <action>
Create `src/lib/dialog.ts` with this exact exported helper:

```ts
export async function pickGameExecutable(): Promise<string | null>
```

Implement it with `open()` from `@tauri-apps/plugin-dialog`, `multiple: false`, `directory: false`, and a filter that allows only `exe`.

Create `src/lib/games.ts` with these exact helpers:
- `formatGameSource(source: GameSource): 'steam' | 'manual'`
- `getInstallRoot(executablePath: string): string`

Create `src/stores/games.ts` with these exact state fields:
- `entries: GameLibraryEntry[]`
- `loadState: 'idle' | 'loading' | 'ready' | 'error'`
- `error: AppError | null`
- `manualExecutablePath: string | null`
- `isManualFormOpen: boolean`

Create these exact store actions:
- `refreshLibrary(): Promise<void>`
- `beginManualRegistration(): Promise<void>`
- `cancelManualRegistration(): void`
- `saveManualGame(input: ManualGameRegistrationInput): Promise<void>`
- `removeManualEntry(gameId: string): Promise<void>`

`beginManualRegistration()` must call `pickGameExecutable()`, store the selected path, and open the confirmation form only when a path was chosen.

Extend `src/i18n/messages.ts` in both `en` and `pt-BR` catalogs with concrete Phase 3 copy for:
- `Refresh library`
- `Add game`
- `No games detected yet`
- `Steam`
- `Manual`
- `Display name`
- `Executable`
- `Save game`
- `Cancel`
- `Remove`
- `Library refreshed`

Create `src/components/games/GameSourceBadge.tsx` to render `Steam` and `Manual` variants using exact modifier classes:
- `.game-source-badge--steam`
- `.game-source-badge--manual`

Create `src/components/games/GameLibraryTable.tsx` that renders a compact table/list with exact columns:
1. Name
2. Source
3. Install path
4. Actions

Create `src/components/games/ManualGameRegistrationForm.tsx` that renders:
- a read-only executable path row
- a required `displayName` input
- a `Save game` button
- a `Cancel` button

Extend `src/index.css` with exact classes for the Phase 3 compact management UI:
- `.game-library-toolbar`
- `.game-library-table`
- `.game-library-row`
- `.game-library-cell`
- `.game-source-badge`
- `.game-source-badge--steam`
- `.game-source-badge--manual`
- `.game-library-empty`
- `.manual-game-form`
  </action>
  <verify>Check that the dialog helper, games store, badges, table, form, and new library strings/classes exist with the exact labels and class names defined above</verify>
  <acceptance_criteria>
    - src/lib/dialog.ts contains 'pickGameExecutable'
    - src/lib/dialog.ts contains '@tauri-apps/plugin-dialog'
    - src/stores/games.ts contains 'beginManualRegistration'
    - src/stores/games.ts contains 'saveManualGame'
    - src/i18n/messages.ts contains 'Refresh library'
    - src/i18n/messages.ts contains 'Add game'
    - src/i18n/messages.ts contains 'Save game'
    - src/components/games/GameSourceBadge.tsx contains 'game-source-badge--steam'
    - src/components/games/GameLibraryTable.tsx contains 'Install path'
    - src/components/games/ManualGameRegistrationForm.tsx contains 'displayName'
    - src/index.css contains '.game-source-badge--manual'
    - src/index.css contains '.manual-game-form'
  </acceptance_criteria>
  <done>Frontend library-management building blocks and bilingual copy are ready for the page layer</done>
</task>

<task type="auto">
  <name>Task 3: Wire LibraryPage to the persisted library flow and add focused frontend tests</name>
  <files>src/pages/LibraryPage.tsx, src/pages/GamesPage.tsx, src/pages/LibraryPage.test.tsx, src/components/games/GameLibraryTable.test.tsx, src/components/games/ManualGameRegistrationForm.test.tsx</files>
  <read_first>src/pages/LibraryPage.tsx, src/pages/GamesPage.tsx, src/stores/games.ts, src/components/games/GameLibraryTable.tsx, src/components/games/ManualGameRegistrationForm.tsx, .planning/phases/03-game-detection/03-CONTEXT.md</read_first>
  <action>
Update `src/pages/LibraryPage.tsx` so that on mount it calls `refreshLibrary()` from `useGamesStore()` when `loadState === 'idle'`.

Turn `LibraryPage` into the Phase 3 management surface with:
- a toolbar containing `Refresh library` and `Add game`
- the compact `GameLibraryTable`
- the inline `ManualGameRegistrationForm` below the toolbar when `isManualFormOpen` is true
- an empty state that shows `No games detected yet` when the library is empty after load

The management table must always distinguish Steam and manual rows visually through `GameSourceBadge`.
The `Actions` cell must:
- show `Remove` only when `entry.removable === true`
- hide remove controls for Steam-promoted or Steam-detected rows

Update `src/pages/GamesPage.tsx` so it no longer reads as an abandoned placeholder. Keep it lightweight for Phase 3 by changing the subtitle/copy to point users to the managed library flow without introducing Phase 4 cover-art or detail-page work.

Create frontend tests with Testing Library:
- `src/pages/LibraryPage.test.tsx` must mock the store or wrappers and assert the page auto-refreshes, renders the toolbar, and shows the empty state / list state correctly
- `src/components/games/GameLibraryTable.test.tsx` must assert Steam and Manual badges render and that only manual rows show `Remove`
- `src/components/games/ManualGameRegistrationForm.test.tsx` must cover the required display-name input plus save/cancel behavior

Do not hardcode new Phase 3 management strings directly inside the page or component JSX; every new user-visible label must come from `useI18n()`.
  </action>
  <verify>Check that LibraryPage calls refreshLibrary, renders the toolbar and table/form, GamesPage no longer reads as a dead placeholder, and the three frontend tests exist with badge/remove/add coverage</verify>
  <acceptance_criteria>
    - src/pages/LibraryPage.tsx contains 'refreshLibrary'
    - src/pages/LibraryPage.tsx contains 'GameLibraryTable'
    - src/pages/LibraryPage.tsx contains 'ManualGameRegistrationForm'
    - src/pages/LibraryPage.tsx contains 'No games detected yet'
    - src/pages/GamesPage.tsx contains 'library'
    - src/pages/LibraryPage.test.tsx contains 'Refresh library'
    - src/components/games/GameLibraryTable.test.tsx contains 'Remove'
    - src/components/games/GameLibraryTable.test.tsx contains 'Steam'
    - src/components/games/GameLibraryTable.test.tsx contains 'Manual'
    - src/components/games/ManualGameRegistrationForm.test.tsx contains 'Save game'
    - src/components/games/ManualGameRegistrationForm.test.tsx contains 'Cancel'
  </acceptance_criteria>
  <done>LibraryPage becomes the compact, persisted management flow for detected and manual games with focused frontend tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml games::tests -- --nocapture` passes
- [ ] `pnpm test -- src/pages/LibraryPage.test.tsx src/components/games/GameLibraryTable.test.tsx src/components/games/ManualGameRegistrationForm.test.tsx` passes
- [ ] `pnpm test` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` passes
- [ ] `LibraryPage` auto-refreshes into a compact management list with source badges and manual-only remove actions
- [ ] Manual registration requires a picked `.exe` plus display-name confirmation before saving
- [ ] Matching manual entries are promoted to Steam origin after refresh instead of duplicating
</verification>

<success_criteria>
- Manual registration persists in SQLite and merges cleanly with Steam detections
- The app exposes a compact management-first library UI with visible source distinction
- Refresh and manual remove behavior match the locked Phase 3 decisions
- The new game-management flow is bilingual-ready and backed by focused frontend tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-detection/03-02-SUMMARY.md`
</output>
