---
phase: 03-game-detection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/commands/games.rs, src/types/ipc.ts, src/stores/games.ts, src/i18n/messages.ts, src/components/games/GameLibraryTable.tsx, src/pages/LibraryPage.tsx, src/components/games/GameLibraryTable.test.tsx, src/pages/LibraryPage.test.tsx, .planning/phases/03-game-detection/03-UAT.md]
autonomous: true
gap_closure: true
requirements: [GAME-06]

must_haves:
  truths:
    - "Manual registrations and Steam detections are presented as separate entries when they point to the same install; adding one must not rewrite or absorb the other"
    - "The library UI keeps Steam and Manual provenance visibly separate instead of promoting one source into the other"
    - "Manual remove continues to affect only manual rows, while Steam rows remain non-removable"
  artifacts:
    - path: "src-tauri/src/commands/games.rs"
      provides: "Reconciliation logic that emits distinct Steam and manual rows instead of a merged effective-source row"
      min_lines: 340
      contains: "reconcile_library"
    - path: "src/types/ipc.ts"
      provides: "Updated game-library entry contract with relationship metadata instead of source promotion"
      contains: "relatedSteamAppId"
    - path: "src/components/games/GameLibraryTable.tsx"
      provides: "Compact list rendering that shows separate Steam and manual rows for the same install"
      contains: "GameSourceBadge"
    - path: "src/pages/LibraryPage.tsx"
      provides: "Management-first page that renders the updated separate-entry model"
      contains: "GameLibraryTable"
    - path: "src/components/games/GameLibraryTable.test.tsx"
      provides: "Regression coverage for separated Steam/manual rows"
      contains: "Added manually"
  key_links:
    - from: "src/stores/games.ts"
      to: "src/lib/tauri.ts"
      via: "load/refresh actions consuming the updated snapshot shape"
      pattern: "refreshGameLibrary"
    - from: "src/pages/LibraryPage.tsx"
      to: "src/components/games/GameLibraryTable.tsx"
      via: "management list composition"
      pattern: "GameLibraryTable"
    - from: "src/components/games/GameLibraryTable.tsx"
      to: "src-tauri/src/commands/games.rs"
      via: "separate source rows rendered from backend snapshot"
      pattern: "source"
---

<objective>
Close the remaining UAT gap from Phase 3 by separating manual and Steam entries instead of reconciling them into one effective-source row.

Purpose: Align the game-library behavior with the user's validated expectation that manual and Steam should not affect each other, while preserving the management-first Phase 3 scope.
Output: backend snapshot logic that emits separate rows, UI updates that render them clearly, and regression tests protecting the new behavior.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-detection/03-CONTEXT.md
@.planning/phases/03-game-detection/03-RESEARCH.md
@.planning/phases/03-game-detection/03-UAT.md
@.planning/phases/03-game-detection/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the merged effective-source model with separate Steam and manual rows</name>
  <files>src-tauri/src/commands/games.rs, src/types/ipc.ts</files>
  <read_first>src-tauri/src/commands/games.rs, src/types/ipc.ts, .planning/phases/03-game-detection/03-UAT.md, .planning/phases/03-game-detection/03-02-PLAN.md, .planning/phases/03-game-detection/03-VERIFICATION.md</read_first>
  <action>
Update `src/types/ipc.ts` and `src-tauri/src/commands/games.rs` so `reconcile_library()` no longer merges a manual registration into the corresponding Steam row.

Keep `manual_game_registrations` as the persisted table and keep Steam detection ephemeral, but change the emitted snapshot rules to these exact outcomes:

1. Every Steam detection always emits its own row:
   - `id = steam:{appid}`
   - `source = 'steam'`
   - `userAdded = false`
   - `removable = false`
   - `executablePath = null`

2. Every manual registration always emits its own row:
   - `id = manual:{registration_id}`
   - `source = 'manual'`
   - `userAdded = true`
   - `removable = true`
   - `executablePath = saved manual executable path`

3. Keep normalized install root matching available only as relationship metadata, not as a merge trigger. Add an optional field to `GameLibraryEntry` in Rust and TypeScript named exactly:
   - `relatedSteamAppId: number | null`

When a manual row shares the same normalized install root as a Steam detection:
   - the manual row keeps `source = 'manual'`
   - the Steam row keeps `source = 'steam'`
   - the manual row gets `relatedSteamAppId = Some(appid)`
   - the Steam row keeps `relatedSteamAppId = None`

Update the reconciliation comments to explicitly say:
- Steam and manual entries remain independent in the returned snapshot
- install-root matching is used only to attach relationship metadata in Phase 3
- remove operations remain scoped to manual rows only

Update backend tests so the old merged-row expectations are removed and replaced with separate-row assertions.
  </action>
  <verify>Check that reconcile_library emits separate Steam/manual rows, GameLibraryEntry includes relatedSteamAppId, and no code path promotes manual entries to source steam anymore</verify>
  <acceptance_criteria>
    - src-tauri/src/commands/games.rs contains 'related_steam_app_id'
    - src-tauri/src/commands/games.rs contains 'Steam and manual entries remain independent'
    - src-tauri/src/commands/games.rs contains 'relatedSteamAppId'
    - src-tauri/src/commands/games.rs does not contain 'user_added: manual_registration.is_some()'
    - src-tauri/src/commands/games.rs contains 'source: GameSource::Manual'
    - src-tauri/src/commands/games.rs contains 'source: GameSource::Steam'
    - src-tauri/src/commands/games.rs contains 'related_steam_app_id: Some'
  </acceptance_criteria>
  <done>The backend snapshot treats Steam and manual as separate entries even when they refer to the same install</done>
</task>

<task type="auto">
  <name>Task 2: Update the library UI to render separate entries and clear relationship hints</name>
  <files>src/stores/games.ts, src/i18n/messages.ts, src/components/games/GameLibraryTable.tsx, src/pages/LibraryPage.tsx</files>
  <read_first>src/stores/games.ts, src/i18n/messages.ts, src/components/games/GameLibraryTable.tsx, src/pages/LibraryPage.tsx, src/types/ipc.ts, .planning/phases/03-game-detection/03-UAT.md</read_first>
  <action>
Extend the frontend snapshot typing in the consuming code to handle the new `relatedSteamAppId` field.

Add this exact copy key to both `en` and `pt-BR` in `src/i18n/messages.ts` under the library table section:
- `linkedToSteamDetection`

Use these exact values:
- EN: `Linked to Steam detection`
- pt-BR: `Vinculado à detecção da Steam`

Update `src/components/games/GameLibraryTable.tsx` so rows render separately with these exact rules:
- Steam rows continue to show the Steam badge and no remove button
- Manual rows continue to show the Manual badge and the remove button
- Manual rows with `relatedSteamAppId !== null` show helper text that makes the relationship visible without merging the rows; use the exact copy key from i18n:
  - `Linked to Steam detection`

Do not render `Added manually` under Steam rows anymore, because that helper text reflected the merged-row model.

Update `src/pages/LibraryPage.tsx` only as needed to support the new separate-row rendering. Keep the same management-first layout, toolbar, refresh behavior, and manual form flow.

If needed, extend `src/stores/games.ts` only to accommodate the updated snapshot shape. Do not change the load/refresh/add/remove action names.
  </action>
  <verify>Check that Steam rows no longer show preserved-manual provenance text, manual rows can show the new linked-to-Steam hint, and remove stays manual-only</verify>
  <acceptance_criteria>
    - src/i18n/messages.ts contains 'Linked to Steam detection'
    - src/i18n/messages.ts contains 'Vinculado à detecção da Steam'
    - src/components/games/GameLibraryTable.tsx contains 'relatedSteamAppId'
    - src/components/games/GameLibraryTable.tsx contains 'Linked to Steam detection'
    - src/components/games/GameLibraryTable.tsx does not contain "entry.source === 'steam' ? entry.userAdded"
    - src/components/games/GameLibraryTable.tsx contains 'entry.removable'
    - src/pages/LibraryPage.tsx contains 'GameLibraryTable'
    - src/stores/games.ts contains 'refreshLibrary'
  </acceptance_criteria>
  <done>The library UI reflects the user's desired separation between Steam and manual entries without breaking the existing flow</done>
</task>

<task type="auto">
  <name>Task 3: Replace merged-row tests with gap-closing regression coverage and update UAT status</name>
  <files>src/components/games/GameLibraryTable.test.tsx, src/pages/LibraryPage.test.tsx, src-tauri/src/commands/games.rs, .planning/phases/03-game-detection/03-UAT.md</files>
  <read_first>src/components/games/GameLibraryTable.test.tsx, src/pages/LibraryPage.test.tsx, src-tauri/src/commands/games.rs, .planning/phases/03-game-detection/03-UAT.md</read_first>
  <action>
Update backend `manual_registry` tests in `src-tauri/src/commands/games.rs` so the previous merged-row expectations are replaced with these exact test names:
- `reconciliation_keeps_manual_and_steam_entries_separate_for_same_install`
- `manual_rows_can_reference_related_steam_app_id_without_losing_manual_source`

Keep the existing add/remove tests.

Update `src/components/games/GameLibraryTable.test.tsx` so it asserts:
- Steam and manual rows both render when they refer to the same install
- the manual row still shows `Remove`
- the manual row shows `Linked to Steam detection`
- the Steam row does not show `Added manually`

Update `src/pages/LibraryPage.test.tsx` only as needed so the page-level expectations match the separate-row model.

Finally, update `.planning/phases/03-game-detection/03-UAT.md` so gap #6 is marked from `status: failed` to `status: planned` and note that a dedicated gap-closure plan `03-03-PLAN.md` now exists.
  </action>
  <verify>Check that the old merged-row tests are gone, the new separate-entry tests exist, and UAT gap #6 points at this new plan</verify>
  <acceptance_criteria>
    - src-tauri/src/commands/games.rs contains 'reconciliation_keeps_manual_and_steam_entries_separate_for_same_install'
    - src-tauri/src/commands/games.rs contains 'manual_rows_can_reference_related_steam_app_id_without_losing_manual_source'
    - src/components/games/GameLibraryTable.test.tsx contains 'Linked to Steam detection'
    - src/components/games/GameLibraryTable.test.tsx contains 'Remove'
    - src/components/games/GameLibraryTable.test.tsx does not contain 'Added manually'
    - .planning/phases/03-game-detection/03-UAT.md contains '03-03-PLAN.md'
    - .planning/phases/03-game-detection/03-UAT.md contains 'status: planned'
  </acceptance_criteria>
  <done>The gap is covered by explicit backend/frontend regression tests and the UAT file points to the closure plan</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml games::tests::manual_registry -- --nocapture` passes
- [ ] `pnpm test -- src/components/games/GameLibraryTable.test.tsx src/pages/LibraryPage.test.tsx` passes
- [ ] `pnpm test` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` passes
- [ ] Steam and manual rows remain visually separate for the same install
- [ ] Manual remove still applies only to manual rows
- [ ] UAT gap #6 is marked planned and references `03-03-PLAN.md`
</verification>

<success_criteria>
- The remaining Phase 3 UAT gap is isolated to one executable plan
- The plan removes the merged effective-source behavior and replaces it with explicit separate-entry behavior
- Backend, UI, and UAT tracking are all covered in the gap closure plan
- The rest of the validated Phase 3 behavior remains untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-detection/03-03-SUMMARY.md`
</output>
