---
phase: 01-foundation-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/src/lib.rs, src-tauri/capabilities/default.json, src/main.tsx, src/App.tsx, package.json, vite.config.ts, tsconfig.json, pnpm-lock.yaml]
autonomous: true
requirements: [UIUX-01]

must_haves:
  truths:
    - "Tauri app window opens on Windows showing React frontend"
    - "pnpm tauri dev launches without errors"
    - "React renders 'Hello OptiHub' in WebView2"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri application configuration"
      contains: "com.optihub.app"
    - path: "src-tauri/src/lib.rs"
      provides: "Rust entry point with command registration"
      contains: "tauri::Builder"
    - path: "src/App.tsx"
      provides: "React root component"
      min_lines: 5
    - path: "package.json"
      provides: "Project dependencies"
      contains: "react"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React DOM render"
      pattern: "import.*App"
---

<objective>
Scaffold the complete Tauri 2 + React + TypeScript + Vite project with pnpm, configured for Windows desktop.

Purpose: Establish the foundational project structure that all other plans and phases build upon.
Output: A working Tauri desktop app that opens a window with React rendering inside WebView2.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 + React + TypeScript project</name>
  <files>package.json, vite.config.ts, tsconfig.json, src/main.tsx, src/App.tsx, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/research/STACK.md</read_first>
  <action>
Run the Tauri scaffold command with pnpm:
```
pnpm create tauri-app@latest ./ --template react-ts --manager pnpm
```

If the scaffold command requires interactive input, use these values:
- Project name: optihub
- Identifier: com.optihub.app
- Frontend: TypeScript / JavaScript
- Package manager: pnpm
- UI template: React
- UI flavor: TypeScript

After scaffolding, verify the generated structure:
- `src/` contains React frontend (main.tsx, App.tsx)
- `src-tauri/` contains Rust backend (src/main.rs or src/lib.rs, Cargo.toml, tauri.conf.json)
- `vite.config.ts` configured for Tauri
- `package.json` has react, react-dom, @tauri-apps/api dependencies

Update `src-tauri/tauri.conf.json`:
- Set `productName` to "OptiHub"
- Set `identifier` to "com.optihub.app"
- Set window title to "OptiHub"
- Set window width to 1280, height to 800
- Set window minWidth to 960, minHeight to 600
- Set window decorations to true
- Set window center to true

Update `src/App.tsx` to render a minimal "Hello OptiHub" placeholder to verify the app works.
  </action>
  <verify>Verify files exist: src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src/main.tsx, src/App.tsx, package.json</verify>
  <acceptance_criteria>
    - src-tauri/tauri.conf.json contains "com.optihub.app"
    - src-tauri/tauri.conf.json contains "OptiHub"
    - package.json contains "@tauri-apps/api"
    - package.json contains "react"
    - src/App.tsx contains "OptiHub"
  </acceptance_criteria>
  <done>Tauri project scaffolded with React + TypeScript, window configured for OptiHub</done>
</task>

<task type="auto">
  <name>Task 2: Install Tailwind CSS 4 and configure for the project</name>
  <files>package.json, src/index.css, vite.config.ts</files>
  <read_first>package.json, vite.config.ts</read_first>
  <action>
Install Tailwind CSS 4 and its Vite plugin:
```
pnpm add -D tailwindcss @tailwindcss/vite
```

Update `vite.config.ts` to include the Tailwind plugin:
```typescript
import tailwindcss from '@tailwindcss/vite';
// Add to plugins array: tailwindcss()
```

Create or update `src/index.css` with:
```css
@import "tailwindcss";
```

Ensure `src/main.tsx` imports `./index.css`.

Update `src/App.tsx` to use a Tailwind class (e.g., `className="bg-gray-900 text-white min-h-screen"`) to verify Tailwind is working.
  </action>
  <verify>Check that vite.config.ts contains tailwindcss plugin and src/index.css contains @import "tailwindcss"</verify>
  <acceptance_criteria>
    - package.json contains "tailwindcss"
    - package.json contains "@tailwindcss/vite"
    - vite.config.ts contains "tailwindcss"
    - src/index.css contains "@import"
    - src/App.tsx contains "bg-gray-900" or similar Tailwind dark class
  </acceptance_criteria>
  <done>Tailwind CSS 4 installed and configured with Vite plugin</done>
</task>

<task type="auto">
  <name>Task 3: Install routing and state dependencies</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Install React Router 7 and Zustand 5 for navigation and state management:
```
pnpm add react-router zustand
```

Install Lucide React for icons:
```
pnpm add lucide-react
```

Verify all dependencies are in package.json after install.
  </action>
  <verify>Check package.json contains react-router, zustand, lucide-react</verify>
  <acceptance_criteria>
    - package.json contains "react-router"
    - package.json contains "zustand"
    - package.json contains "lucide-react"
  </acceptance_criteria>
  <done>All frontend dependencies installed: react-router, zustand, lucide-react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` completes without errors
- [ ] All expected files exist (src-tauri/*, src/*, package.json, vite.config.ts)
- [ ] package.json contains all required dependencies
- [ ] Tailwind CSS plugin configured in vite.config.ts
</verification>

<success_criteria>
- All tasks completed
- Project scaffolded with Tauri 2 + React + TypeScript + Vite
- Tailwind CSS 4 installed and configured
- React Router, Zustand, and Lucide React installed
- tauri.conf.json configured for OptiHub (identifier, window size, title)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-shell/01-01-SUMMARY.md`
</output>
