---
phase: 01-foundation-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/index.css, src/App.tsx, src/layouts/AppShell.tsx, src/components/Sidebar.tsx, src/pages/HomePage.tsx, src/pages/LibraryPage.tsx, src/pages/GamesPage.tsx, src/pages/OptimizationsPage.tsx, src/pages/ToolsPage.tsx, src/pages/PresetsPage.tsx, src/pages/DiagnosticsPage.tsx, src/pages/SettingsPage.tsx, src/pages/CreditsPage.tsx, src/stores/ui.ts]
autonomous: true
requirements: [UIUX-01, UIUX-02, UIUX-04]

must_haves:
  truths:
    - "App displays premium dark theme with deep background colors"
    - "Sidebar shows all 9 navigation items with icons"
    - "Clicking sidebar items navigates to corresponding pages"
    - "Active sidebar item is visually highlighted"
    - "All interactive elements show hover/focus states"
    - "Credits/Licenses page renders with placeholder content"
  artifacts:
    - path: "src/layouts/AppShell.tsx"
      provides: "Main layout with sidebar and content area"
      min_lines: 30
      contains: "Sidebar"
    - path: "src/components/Sidebar.tsx"
      provides: "Navigation sidebar with 9 routes"
      min_lines: 50
      contains: "Credits"
    - path: "src/index.css"
      provides: "Design system tokens and base styles"
      min_lines: 40
      contains: "--color-bg"
    - path: "src/pages/CreditsPage.tsx"
      provides: "Credits placeholder page"
      contains: "Credits"
    - path: "src/stores/ui.ts"
      provides: "UI state store with Zustand"
      contains: "create"
  key_links:
    - from: "src/App.tsx"
      to: "src/layouts/AppShell.tsx"
      via: "React Router layout route"
      pattern: "AppShell"
    - from: "src/layouts/AppShell.tsx"
      to: "src/components/Sidebar.tsx"
      via: "Component composition"
      pattern: "Sidebar"
    - from: "src/components/Sidebar.tsx"
      to: "react-router"
      via: "NavLink navigation"
      pattern: "NavLink|useNavigate|Link"
---

<objective>
Build the premium dark theme design system, sidebar navigation with all 9 routes, and page placeholders — creating the visual identity and navigation backbone of OptiHub.

Purpose: Establish the app's premium gamer-launcher look and all navigation routes so downstream phases can add content to existing pages.
Output: Working sidebar layout, all routes navigable, dark gaming theme applied, Credits placeholder present.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design system tokens and base styles</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>
Replace the content of `src/index.css` with the OptiHub design system. Use Tailwind CSS 4's `@theme` directive for custom properties.

Define these **exact** CSS custom properties in `@theme`:
```css
@import "tailwindcss";

@theme {
  /* Background colors — deep dark with slight blue/purple undertone */
  --color-bg-primary: #0a0a0f;
  --color-bg-secondary: #111118;
  --color-bg-surface: #1a1a2e;
  --color-bg-surface-hover: #22223a;
  --color-bg-elevated: #1e1e36;
  
  /* Accent — cyberpunk purple/blue */
  --color-accent-primary: #6c5ce7;
  --color-accent-secondary: #a29bfe;
  --color-accent-glow: rgba(108, 92, 231, 0.3);
  
  /* Text */
  --color-text-primary: #e8e8f0;
  --color-text-secondary: #8888a0;
  --color-text-muted: #555570;
  
  /* Borders */
  --color-border-subtle: rgba(255, 255, 255, 0.06);
  --color-border-default: rgba(255, 255, 255, 0.10);
  --color-border-accent: rgba(108, 92, 231, 0.4);
  
  /* Status */
  --color-status-safe: #00d68f;
  --color-status-limited: #ffaa00;
  --color-status-experimental: #ff4757;
  
  /* Sidebar */
  --sidebar-width: 240px;
  
  /* Spacing scale */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  
  /* Border radius */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 16px;
  
  /* Transitions */
  --transition-fast: 150ms ease;
  --transition-normal: 250ms ease;
}
```

Add base styles after the @theme block:
```css
/* Base resets */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #root {
  height: 100%;
  width: 100%;
  overflow: hidden;
}

body {
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: var(--color-text-muted);
  border-radius: 3px;
}
::-webkit-scrollbar-thumb:hover {
  background: var(--color-text-secondary);
}

/* Focus visible for accessibility */
:focus-visible {
  outline: 2px solid var(--color-accent-primary);
  outline-offset: 2px;
}
```

Download and store Inter font locally in `src/assets/fonts/` for offline-first. If download not feasible, use system font stack with Inter as first choice from local system fonts.
  </action>
  <verify>Check that src/index.css contains --color-bg-primary, --color-accent-primary, @theme directive</verify>
  <acceptance_criteria>
    - src/index.css contains "--color-bg-primary: #0a0a0f"
    - src/index.css contains "--color-accent-primary: #6c5ce7"
    - src/index.css contains "--color-text-primary: #e8e8f0"
    - src/index.css contains "--sidebar-width: 240px"
    - src/index.css contains "@import" (Tailwind import)
    - src/index.css contains "@theme"
    - src/index.css contains ":focus-visible"
  </acceptance_criteria>
  <done>Design system tokens defined, base styles applied, scrollbar styled, focus states configured</done>
</task>

<task type="auto">
  <name>Task 2: Build sidebar component and app shell layout</name>
  <files>src/components/Sidebar.tsx, src/layouts/AppShell.tsx, src/stores/ui.ts</files>
  <read_first>src/index.css</read_first>
  <action>
Create `src/stores/ui.ts` — Zustand store for UI state:
```typescript
import { create } from 'zustand';

interface UIState {
  sidebarCollapsed: boolean;
  toggleSidebar: () => void;
}

export const useUIStore = create<UIState>((set) => ({
  sidebarCollapsed: false,
  toggleSidebar: () => set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
}));
```

Create `src/components/Sidebar.tsx` with these exact navigation items, in this order, using Lucide React icons:

1. **Home** — `Home` icon — route: `/`
2. **Library** — `Library` icon — route: `/library`
3. **Games** — `Gamepad2` icon — route: `/games`
4. **Optimizations** — `Zap` icon — route: `/optimizations`
5. **Tools** — `Wrench` icon — route: `/tools`
6. **Presets** — `SlidersHorizontal` icon — route: `/presets`
7. **Diagnostics** — `Activity` icon — route: `/diagnostics`

--- (visual divider) ---

8. **Settings** — `Settings` icon — route: `/settings`
9. **Credits** — `Award` icon — route: `/credits`

Sidebar specifications:
- Width: 240px (use var(--sidebar-width))
- Background: var(--color-bg-secondary)
- Right border: 1px solid var(--color-border-subtle)
- Logo area at top: "OptiHub" text with accent color, font-weight 700, font-size 20px
- Nav items: height 40px, padding 12px horizontal, gap 4px between items
- Active state: background var(--color-bg-surface), left border 3px solid var(--color-accent-primary), text color var(--color-text-primary)
- Hover state: background var(--color-bg-surface-hover), transition var(--transition-fast)
- Inactive text: var(--color-text-secondary)
- Icon size: 20px, gap 12px between icon and label
- Divider before Settings: 1px solid var(--color-border-subtle), margin 8px vertical
- Use `NavLink` from react-router for navigation with active class detection

Create `src/layouts/AppShell.tsx`:
- Flexbox layout: sidebar (fixed left) + content area (flex-1, scrollable)
- Content area: background var(--color-bg-primary), padding 24px, overflow-y auto
- Use `<Outlet />` from react-router for nested route content
- Full height layout (100vh)
  </action>
  <verify>Check Sidebar.tsx contains all 9 NavLink items including Credits route</verify>
  <acceptance_criteria>
    - src/components/Sidebar.tsx contains "NavLink"
    - src/components/Sidebar.tsx contains "/credits"
    - src/components/Sidebar.tsx contains "/settings"
    - src/components/Sidebar.tsx contains "/diagnostics"
    - src/components/Sidebar.tsx contains "/optimizations"
    - src/components/Sidebar.tsx contains "OptiHub"
    - src/layouts/AppShell.tsx contains "Outlet"
    - src/layouts/AppShell.tsx contains "Sidebar"
    - src/stores/ui.ts contains "create"
    - src/stores/ui.ts contains "sidebarCollapsed"
  </acceptance_criteria>
  <done>Sidebar with all 9 routes rendered, AppShell layout with Outlet, UI store created</done>
</task>

<task type="auto">
  <name>Task 3: Create page placeholders and configure routing</name>
  <files>src/App.tsx, src/pages/HomePage.tsx, src/pages/LibraryPage.tsx, src/pages/GamesPage.tsx, src/pages/OptimizationsPage.tsx, src/pages/ToolsPage.tsx, src/pages/PresetsPage.tsx, src/pages/DiagnosticsPage.tsx, src/pages/SettingsPage.tsx, src/pages/CreditsPage.tsx</files>
  <read_first>src/layouts/AppShell.tsx, src/components/Sidebar.tsx</read_first>
  <action>
Create 9 page components in `src/pages/`, each as a placeholder with the page title and a brief description of what will be built there:

1. `HomePage.tsx` — "Welcome to OptiHub" with subtitle "Your PC game optimization hub"
2. `LibraryPage.tsx` — "Game Library" with subtitle "Your detected and added games will appear here"
3. `GamesPage.tsx` — "Games" with subtitle "Browse and manage your game collection"
4. `OptimizationsPage.tsx` — "Optimizations" with subtitle "Active and available optimizations"
5. `ToolsPage.tsx` — "Tools" with subtitle "Detected optimization tools and their status"
6. `PresetsPage.tsx` — "Presets" with subtitle "Quality, Balanced, and Performance presets"
7. `DiagnosticsPage.tsx` — "Diagnostics" with subtitle "System hardware and diagnostics overview"
8. `SettingsPage.tsx` — "Settings" with subtitle "Configure OptiHub preferences"
9. `CreditsPage.tsx` — "Credits & Licenses" with subtitle "OptiHub uses the following open-source projects and tools" and a note: "Full credits, licenses, and attribution information will be listed here. OptiHub is committed to proper licensing and attribution for all integrated tools."

Each page should:
- Use a consistent layout: title (h1, 24px, font-weight 600), subtitle (text-secondary, 14px), separated by 8px
- Have a unique `id` attribute on the root div for testing (e.g., `id="page-home"`, `id="page-credits"`)
- Use design token colors from the CSS variables

Update `src/App.tsx` to configure React Router with:
- BrowserRouter (or MemoryRouter for Tauri — use `BrowserRouter` if Tauri dev server supports it, otherwise `MemoryRouter`)
- Layout route using `AppShell` as the element
- Nested routes for all 9 pages:
  - `/` → HomePage
  - `/library` → LibraryPage
  - `/games` → GamesPage
  - `/optimizations` → OptimizationsPage
  - `/tools` → ToolsPage
  - `/presets` → PresetsPage
  - `/diagnostics` → DiagnosticsPage
  - `/settings` → SettingsPage
  - `/credits` → CreditsPage

Note: For Tauri desktop apps, prefer `BrowserRouter` as Tauri's dev server handles routing. If issues arise, switch to `MemoryRouter`.
  </action>
  <verify>Check that App.tsx contains all 9 route paths and CreditsPage.tsx contains compliance text</verify>
  <acceptance_criteria>
    - src/App.tsx contains "BrowserRouter" or "MemoryRouter"
    - src/App.tsx contains "/credits"
    - src/App.tsx contains "/settings"
    - src/App.tsx contains "/diagnostics"
    - src/App.tsx contains "AppShell"
    - src/pages/CreditsPage.tsx contains "Credits"
    - src/pages/CreditsPage.tsx contains "Licenses" or "licenses"
    - src/pages/CreditsPage.tsx contains "attribution" or "Attribution"
    - src/pages/HomePage.tsx contains "OptiHub"
    - src/pages/HomePage.tsx contains "page-home"
  </acceptance_criteria>
  <done>All 9 page placeholders created, routing configured with AppShell layout, Credits page has compliance placeholder text</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 9 page files exist in src/pages/
- [ ] AppShell.tsx renders Sidebar + Outlet
- [ ] App.tsx has all 9 routes configured
- [ ] Sidebar.tsx has all 9 NavLink items with correct routes
- [ ] Credits page has attribution/licensing placeholder text
- [ ] Design tokens defined in index.css
- [ ] UI store created with Zustand
</verification>

<success_criteria>
- All tasks completed
- Premium dark theme applied via design system tokens
- Sidebar shows all 9 navigation items (Home, Library, Games, Optimizations, Tools, Presets, Diagnostics, Settings, Credits)
- All sidebar items navigate to their pages
- Active sidebar item visually highlighted with accent color
- All elements have hover/focus states
- Credits page has compliance placeholder content
- Design system ready for downstream phases to use
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-shell/01-02-SUMMARY.md`
</output>
