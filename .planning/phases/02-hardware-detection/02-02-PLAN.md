---
phase: 02-hardware-detection
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/i18n/messages.ts, src/lib/hardware.ts, src/stores/hardware.ts, src/components/hardware/HardwareCard.tsx, src/components/hardware/HardwareSummaryWidget.tsx, src/components/hardware/DisplayOverrideForm.tsx, src/pages/DiagnosticsPage.tsx, src/pages/HomePage.tsx, src/pages/DiagnosticsPage.test.tsx, src/components/hardware/DisplayOverrideForm.test.tsx, src/pages/HomePage.hardware.test.tsx, src/index.css]
autonomous: true
requirements: [HDWR-01, HDWR-02, HDWR-03, HDWR-04, HDWR-05, HDWR-06]

must_haves:
  truths:
    - "Diagnostics renders grouped CPU, GPU, Memory, and Display cards from live hardware snapshot data"
    - "Display card supports inline manual override with explicit detected vs manual state and a reset path"
    - "Home shows a concise four-item hardware summary widget alongside the IPC health card"
    - "All new hardware UI copy is sourced from the i18n catalogs in English and pt-BR"
  artifacts:
    - path: "src/stores/hardware.ts"
      provides: "Hardware load state plus persisted display override state"
      contains: "persist"
    - path: "src/pages/DiagnosticsPage.tsx"
      provides: "Grouped hardware cards with manual display override flow"
      min_lines: 80
      contains: "DisplayOverrideForm"
    - path: "src/pages/HomePage.tsx"
      provides: "Hardware summary widget integrated into the Home shell"
      contains: "HardwareSummaryWidget"
    - path: "src/i18n/messages.ts"
      provides: "Bilingual hardware labels and status copy"
      contains: "hardwareSummary"
  key_links:
    - from: "src/stores/hardware.ts"
      to: "src/lib/tauri.ts"
      via: "loadSnapshot calling getHardwareSnapshot"
      pattern: "getHardwareSnapshot"
    - from: "src/pages/DiagnosticsPage.tsx"
      to: "src/stores/hardware.ts"
      via: "hardware snapshot and override selectors"
      pattern: "useHardwareStore"
    - from: "src/pages/HomePage.tsx"
      to: "src/components/hardware/HardwareSummaryWidget.tsx"
      via: "summary widget composition"
      pattern: "HardwareSummaryWidget"
---

<objective>
Build the Diagnostics and Home hardware UI for Phase 2: grouped cards, inline monitor override, concise Home summary, and bilingual shell copy for the new hardware surfaces.

Purpose: Turn the backend hardware snapshot into the user-facing Diagnostics experience and Home summary required by the roadmap.
Output: live Diagnostics cards, manual display override flow, Home hardware summary widget, and focused frontend tests.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardware-detection/02-CONTEXT.md
@.planning/phases/02-hardware-detection/02-RESEARCH.md
@.planning/phases/02-hardware-detection/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hardware i18n copy, effective-display helpers, and persisted state</name>
  <files>src/i18n/messages.ts, src/lib/hardware.ts, src/stores/hardware.ts</files>
  <read_first>src/i18n/messages.ts, src/stores/ui.ts, src/lib/tauri.ts, src/types/ipc.ts, .planning/phases/02-hardware-detection/02-CONTEXT.md</read_first>
  <action>
Extend `src/i18n/messages.ts` in both `en` and `pt-BR` catalogs with exact hardware copy groups under:
- `pages.diagnostics.hardware`
- `pages.home.hardwareSummary`

Include concrete labels for:
- Card titles: CPU, GPU, Memory, Display
- Field labels: Model, Physical cores, Logical threads, Vendor, VRAM, Total RAM, Resolution, Refresh rate, Source
- Status labels: Detected, Manual override active, Unavailable
- Override controls: Use manual override, Width, Height, Refresh rate, Apply override, Reset to detected
- Load / error copy for Diagnostics and Home summary

Create `src/lib/hardware.ts` with these exact exported helpers:
- `formatBytesToGiB(bytes: number | null): string`
- `formatDisplayMode(widthPx: number | null, heightPx: number | null, refreshHz: number | null): string`
- `getHardwareFieldStatus(value: unknown, isOverridden?: boolean): 'detected' | 'overridden' | 'unavailable'`
- `getEffectiveDisplaySnapshot(detected: DisplaySnapshot, override: ManualDisplayOverride): DisplaySnapshot`

Create `src/stores/hardware.ts` using Zustand plus `persist` middleware with the exact storage key `optihub.hardware.display-override`. Define these exact state fields:
- `snapshot: HardwareSnapshot | null`
- `loadState: 'idle' | 'loading' | 'ready' | 'error'`
- `error: AppError | null`
- `displayOverride: ManualDisplayOverride`

Define these exact actions:
- `loadSnapshot(): Promise<void>`
- `setDisplayOverride(override: ManualDisplayOverride): void`
- `clearDisplayOverride(): void`

`loadSnapshot()` must call `getHardwareSnapshot()` from `src/lib/tauri.ts`, and `displayOverride` must store only user-entered display values, never mutate the detected snapshot in place.
  </action>
  <verify>Check that the i18n file contains hardware labels in both locales and the hardware store exposes loadSnapshot plus override actions</verify>
  <acceptance_criteria>
    - src/i18n/messages.ts contains 'hardwareSummary'
    - src/i18n/messages.ts contains 'Use manual override'
    - src/i18n/messages.ts contains 'Manual override active'
    - src/i18n/messages.ts contains 'Detected'
    - src/lib/hardware.ts contains 'formatBytesToGiB'
    - src/lib/hardware.ts contains 'getEffectiveDisplaySnapshot'
    - src/stores/hardware.ts contains 'optihub.hardware.display-override'
    - src/stores/hardware.ts contains 'loadSnapshot'
    - src/stores/hardware.ts contains 'clearDisplayOverride'
    - src/stores/hardware.ts contains 'persist'
  </acceptance_criteria>
  <done>Hardware copy, formatting helpers, and persisted override state are ready for the UI layer</done>
</task>

<task type="auto">
  <name>Task 2: Build grouped hardware components and the inline display override UI</name>
  <files>src/components/hardware/HardwareCard.tsx, src/components/hardware/HardwareSummaryWidget.tsx, src/components/hardware/DisplayOverrideForm.tsx, src/index.css</files>
  <read_first>src/index.css, src/pages/HomePage.tsx, .planning/phases/02-hardware-detection/02-CONTEXT.md, .planning/phases/01-foundation-app-shell/01-02-PLAN.md</read_first>
  <action>
Create `src/components/hardware/HardwareCard.tsx` as a reusable grouped-card wrapper that:
- accepts `title`, `subtitle`, and `children`
- uses the existing `page-card` visual language
- renders field rows with labels on the left and values on the right

Create `src/components/hardware/DisplayOverrideForm.tsx` with this exact field set:
- `widthPx`
- `heightPx`
- `refreshHz`

The form must render inline inside the Display card and expose these exact controls:
- button: `Use manual override`
- button: `Apply override`
- button: `Reset to detected`

Create `src/components/hardware/HardwareSummaryWidget.tsx` that renders four compact summary rows in this order:
1. CPU
2. GPU
3. Memory
4. Display

Update `src/index.css` with explicit styles for:
- `.hardware-grid`
- `.hardware-card`
- `.hardware-field`
- `.hardware-field__value`
- `.hardware-status`
- `.hardware-status--detected`
- `.hardware-status--overridden`
- `.hardware-status--unavailable`
- `.hardware-summary`
- `.hardware-summary__item`

Use existing design tokens only. Detected status should use the safe green palette, manual override should use the accent palette, and unavailable should use the muted text / border palette.
  </action>
  <verify>Check that the hardware components exist, the inline override form exposes the exact button labels, and index.css defines hardware status classes</verify>
  <acceptance_criteria>
    - src/components/hardware/HardwareCard.tsx contains 'export function HardwareCard'
    - src/components/hardware/DisplayOverrideForm.tsx contains 'Use manual override'
    - src/components/hardware/DisplayOverrideForm.tsx contains 'Apply override'
    - src/components/hardware/DisplayOverrideForm.tsx contains 'Reset to detected'
    - src/components/hardware/HardwareSummaryWidget.tsx contains 'CPU'
    - src/index.css contains '.hardware-status--detected'
    - src/index.css contains '.hardware-status--overridden'
    - src/index.css contains '.hardware-status--unavailable'
    - src/index.css contains '.hardware-summary__item'
  </acceptance_criteria>
  <done>Reusable hardware UI pieces and status styling are available for Diagnostics and Home</done>
</task>

<task type="auto">
  <name>Task 3: Wire Diagnostics and Home to live hardware state and add frontend tests</name>
  <files>src/pages/DiagnosticsPage.tsx, src/pages/HomePage.tsx, src/pages/DiagnosticsPage.test.tsx, src/components/hardware/DisplayOverrideForm.test.tsx, src/pages/HomePage.hardware.test.tsx</files>
  <read_first>src/pages/DiagnosticsPage.tsx, src/pages/HomePage.tsx, src/components/hardware/HardwareCard.tsx, src/components/hardware/DisplayOverrideForm.tsx, src/components/hardware/HardwareSummaryWidget.tsx, src/stores/hardware.ts</read_first>
  <action>
Update `src/pages/DiagnosticsPage.tsx` so that on mount it calls `loadSnapshot()` from `useHardwareStore()` and renders exactly four grouped cards in this order:
1. CPU
2. GPU
3. Memory
4. Display

Each card must always render, even when some values are missing. Missing values must show the i18n "Unavailable" copy instead of hiding the row.

The Display card must:
- show detected monitor name / resolution / refresh rate first
- show a status pill that reads either `Detected`, `Manual override active`, or `Unavailable`
- embed `DisplayOverrideForm`
- apply the effective display snapshot from `getEffectiveDisplaySnapshot`

Update `src/pages/HomePage.tsx` to keep the existing IPC status card and add `HardwareSummaryWidget` below it. The summary widget must use the effective display snapshot, not the raw detected display values, so the Home page mirrors the override state.

Create frontend tests with Testing Library:
- `src/pages/DiagnosticsPage.test.tsx` must mock `getHardwareSnapshot` and assert all four cards render plus unavailable states remain visible
- `src/components/hardware/DisplayOverrideForm.test.tsx` must cover apply + reset behavior
- `src/pages/HomePage.hardware.test.tsx` must assert the compact CPU / GPU / Memory / Display summary renders after hardware load

Do not hardcode new English-only UI strings in the pages or components; all new hardware labels and statuses must come from `useI18n()`.
  </action>
  <verify>Check that Diagnostics renders four cards, Home imports HardwareSummaryWidget, and the three frontend test files exist with hardware assertions</verify>
  <acceptance_criteria>
    - src/pages/DiagnosticsPage.tsx contains 'loadSnapshot'
    - src/pages/DiagnosticsPage.tsx contains 'DisplayOverrideForm'
    - src/pages/DiagnosticsPage.tsx contains 'HardwareCard'
    - src/pages/HomePage.tsx contains 'HardwareSummaryWidget'
    - src/pages/DiagnosticsPage.test.tsx contains 'getHardwareSnapshot'
    - src/pages/DiagnosticsPage.test.tsx contains 'Unavailable'
    - src/components/hardware/DisplayOverrideForm.test.tsx contains 'Reset to detected'
    - src/pages/HomePage.hardware.test.tsx contains 'CPU'
    - src/pages/HomePage.hardware.test.tsx contains 'Display'
  </acceptance_criteria>
  <done>Diagnostics and Home are wired to live hardware state with inline override UX and focused frontend tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- src/pages/DiagnosticsPage.test.tsx src/components/hardware/DisplayOverrideForm.test.tsx src/pages/HomePage.hardware.test.tsx` passes
- [ ] `pnpm test` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` passes
- [ ] Diagnostics renders four grouped hardware cards from one snapshot load
- [ ] Display override changes the effective display shown on Diagnostics and Home without mutating detected values
- [ ] All new hardware copy comes from i18n catalogs in English and pt-BR
</verification>

<success_criteria>
- Diagnostics page becomes the source-of-truth hardware view for CPU, GPU, Memory, and Display
- Manual display override is inline, explicit, and reversible
- Home shows a compact hardware summary widget
- Hardware UI stays bilingual-ready and avoids new hardcoded shell strings
</success_criteria>

<output>
After completion, create `.planning/phases/02-hardware-detection/02-02-SUMMARY.md`
</output>
