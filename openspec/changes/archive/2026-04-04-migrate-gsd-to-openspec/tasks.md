## 1. Audit and mapping

- [x] 1.1 Inventory `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` as the migration source set.
- [x] 1.2 Map each legacy requirement group and roadmap phase to its OpenSpec capability owner.
- [x] 1.3 Decide and document how completed GSD phases remain traceable after OpenSpec becomes the active planning workflow.

## 2. Seed the OpenSpec baseline

- [x] 2.1 Create the `planning-governance` spec and capture the repository's post-migration planning rules.
- [x] 2.2 Create baseline capability specs for `app-shell-ui`, `hardware-detection`, `game-library`, and `metadata-cache` from the current product requirements.
- [x] 2.3 Create baseline capability specs for `tool-integration-deployment`, `recommendation-engine`, `preset-system`, `apply-restore`, and `compliance-credits`.

## 3. Update contributor workflow

- [x] 3.1 Update repository guidance so future requirement changes are proposed through OpenSpec instead of new GSD phase or plan artifacts.
- [x] 3.2 Add a migration note that explains where historical `.planning` evidence lives and how it maps to OpenSpec capabilities.
- [x] 3.3 Align README or contributor-facing workflow docs with the new OpenSpec-first planning flow.

## 4. Validate and transition

- [x] 4.1 Run OpenSpec status or validation checks and resolve any schema or formatting issues in the migrated artifacts.
- [x] 4.2 Mark `.planning` as historical or transitional according to the chosen migration policy without deleting needed execution history.
- [x] 4.3 Update `.agent/CONTINUITY.md` and the transition summary with the adopted migration decisions and remaining follow-ups.
